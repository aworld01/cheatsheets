/*
When we use var, we can stored only one value at a time.
When we feel like storing multiple values in one variable then instead of var, we will use an Array.
Arrays are the prototype of this class.

0 index / lower index / lower boundary
-1 index / upper index / upper boundary


TOPICS
Traversal of an Array
Searching and Filter in an Array
How to Sort and Compare an Array
Insert, Add, Replace, and Delete Elements in Array (CRUD)
Map(), Reduce(), Filter()
*/

let Friends = []; //creating an empty array
var data = ["Rahul", "Mahesh", "Abid", "Subhan", "Akbar", 25, false, NaN, undefined]; //You can store any datatype

console.log(Friends);
console.log(data);