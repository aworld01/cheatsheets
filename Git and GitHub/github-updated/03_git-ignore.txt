touch '.gitignore': to create an empty file.
ls -a: to check '.gitignore' file.

nano .gitignore: to edit '.gitignore' file.
	
	properties/
	*.log
	passwords.txt
	.gitignore

Ctrl+S, Ctrl+X

cat '.gitignore': to check files data